#!/usr/bin/env bash
set -e

if command -v codium >/dev/null 2>&1; then
    exec codium "$HOME/hudut-linux" "$HOME/hudut-python-lab"
else
    echo "VSCodium kurulu değil."
    read -r -p "Kapatmak için Enter..."
fi
